<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.a3thproject">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:label="main_page"/>
        <activity android:name=".MapInfoListActivity" android:label="MapInfoList" />
        <activity android:name=".MapTacticsListActivity" android:label="MapTacticsList" />
        <activity android:name=".DirectMessageActivity" android:label="DirectMessage"/>
        <activity android:name=".DMDetailActivity" android:label="DM_Detail"/>
        <!--<activity android:name=".MypageActivity" android:label="Mypage" />-->
    </application>

</manifest>